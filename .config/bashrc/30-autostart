numbers=(8 12 20 21)

random_index=$((RANDOM % ${#numbers[@]}))

random_number=${numbers[$random_index]}

if [[ $(tty) == *"pts"* ]]; then
    fastfetch --config "examples/$random_number"
else
    echo
    if [ -f /bin/hyprctl ]; then
	echo "Start Hyprland with command Hyprland"
    fi
fi
